export const timeStamps : object = {
    createdAt: {
        type: Date, 
        required: true,
        default: Date()
    },
    updatedAt: {
        type: Date, 
        required: true,
        default: Date()
    }
}